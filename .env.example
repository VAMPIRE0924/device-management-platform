# Docker image published by this project.
DMP_IMAGE=vampirerune/device-management-platform:latest

# Keep the service on loopback when a reverse proxy runs on the same host.
DMP_BIND_ADDRESS=127.0.0.1
DMP_HOST_PORT=8088
TZ=Asia/Shanghai
