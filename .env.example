# Docker image published by this project.
I5CLOUD_IMAGE=vampirerune/device-management-platform:latest

# Keep the service on loopback when a reverse proxy runs on the same host.
I5CLOUD_BIND_ADDRESS=127.0.0.1
I5CLOUD_HOST_PORT=8088
TZ=Asia/Shanghai
