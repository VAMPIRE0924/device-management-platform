# 更新记录

## v1.0.11 - 2026-08-17

- 修复设备“保存配置”被隐藏的 WebSSH 必填校验静默拦截的问题；未配置凭据的 SSH 入口和 TCP 服务现在均可正常保存。
- Web 入口保持独立子域名与上游根路径不变，避免路径前缀改写破坏绝对路径、WebSocket、Service Worker、CSP 或 Origin 校验。
- Web 按钮改为用户手势触发的平台同源表单，由平台中转页向设备域名发送一次性顶层 POST，彻底移除 about:blank 跨域脚本跳转，授权值不进入 URL、请求日志或 Referer。
- 每次创建 Web 访问会话时生成独立的 `web-` 加 8 位密码学随机域名前缀；不使用共享入口池或固定派生域名，可扩展到大量项目、设备与 Web 服务，并避免单个被 Safe Browsing 误报的域名被永久复用。
- 每个随机入口天然使用独立浏览器 Origin，设备 Cookie 保持原始名称与语义；平台授权 Cookie、上游 Cookie、WebSocket 和设备登录状态不会跨会话、用户或设备泄漏。
- 平台中转页、设备域授权完成页和上游 HTML 页面均明确标识 I5CLOUD 与目标内网设备的代理关系；授权页和上游响应禁止索引，独立访问监听器只接受当前 8 位随机路由标签。
- 严格执行面板域名与反代域名的 Host 边界：配置面板地址后，面板只接受该域名；反代根域、格式错误的随机子域及其他 Host 均直接拒绝，不再回落到面板 SPA。
- Web 与 WebSSH 会话按真实代理流量续期，闲置上限不超过 NPS SOCKS 的 30 分钟无流量回收时间；闲置连接会关闭，过期会话不能被新流量复活，平台不再在 SOCKS 拨号失败后重复启动通道。
- 终止访问会话时立即关闭该会话现有的 Web、WebSocket 与 WebSSH 连接，并阻止已通过数据库校验但尚未拨号的并发请求继续建立连接。
- 删除同源路径代理、HTML/CSS/JavaScript 根路径改写及对应兼容测试；Web 反代只能通过配置的随机子域名访问，面板域名上的内部 `/access/web/` 路径一律拒绝。
- 反代地址输入框保留不可编辑的 `*.` 前缀，并使其与域名值使用完全一致的输入框文字样式，不再显示为独立徽章。
- 运行监控的活动访问会话列表改为显示实际 Web 域名前缀，不再展示无助于定位入口的内部会话 ID；非 Web 会话显示 `—`。
- 完成 Go 全量测试、14 项前端契约、本地与容器黑盒验收以及容器漏洞扫描。

## v1.0.0 - 2026-08-15

- 发布 设备管理平台 首个正式容器版本。
- 完成重构版 NPS 节点、Client 和 SOCKS 通道接入。
- 完成客户项目、设备发现、多 Web 服务、WebSSH、权限、策略、监控和审计。
- 完成节点与 SSH 密码加密持久化、MFA、SQLite 一致性备份和离线恢复。
- 完成独立泛域名 Web 网关以及 HTTP、HTTPS、WebSocket 和重定向兼容。
- 统一正式版界面、列表分页、固定应用框架和响应式布局。
- 清理旧兼容字段、重复接口、未生效配置和测试预览内容。
