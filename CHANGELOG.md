# 更新记录

## v1.0.7 - 2026-08-15

- 登录会话默认闲置 15 分钟、最长 12 小时，后台轮询不再错误续期；两项时长均可在系统设置中热更新。
- Web 与 WebSSH 访问会话不再使用固定 5 分钟期限，且绝不超过创建它的平台登录会话有效期。
- 加固随机子域名的一次性授权、用户/项目/设备隔离、来源地址绑定、Cookie/Host/重定向/WebSocket 边界和审计脱敏。
- 系统设置重构为登录安全、邮件服务、访问与证书、数据与维护四个页签；普通设置即时生效，监听端口与证书可在页面内重载，无需手工重启 Docker。
- 修复全局帮助提示重复显示，统一页面字号与设置界面布局，并补充跨项目访问隔离和正式浏览器验收。

## v1.0.6 - 2026-08-15

- 面板域名与 Web 反代泛域名可分别配置证书和私钥路径，复用 HTTPS 端口时通过 SNI 自动选择证书。
- 反代域名可复用面板 HTTP/HTTPS 端口，也可配置一对不冲突的独立端口。
- 修复面板使用 4043 等非默认 HTTPS 端口时，Web 会话地址丢失端口导致无法访问的问题。
- 重构 HTTP/HTTPS 监听器创建与启停逻辑，增加双证书、双 SNI、root-only 只读挂载和自定义端口验收。

## v1.0.5 - 2026-08-15

- 修复 NAS 上以 root-only 权限只读挂载证书时，平台进程无法读取证书而无限重启的问题。
- 系统设置中填写证书和私钥路径后，平台可直接使用群晖只读挂载的证书，无需修改宿主机权限。
- 增加 root-only 证书挂载、HTTPS 启动和 Secure Cookie 容器验收。
- 升级 `golang.org/x/crypto` 至 `v0.55.0`。

## v1.0.4 - 2026-08-15

- 修复正式模式通过 HTTP 访问时登录 Cookie 被浏览器拒绝、登录后无法进入平台的问题。
- 登录 Cookie 现在按实际 HTTP/HTTPS 请求自动设置安全属性，不再要求部署者配置 Cookie 开关。
- 容器默认 HTTP 端口改为 80；配置证书后同时在 443 提供 HTTPS。
- 系统设置新增 HTTP 与 HTTPS 端口设置，保存后重启容器生效。
- Compose 不再声明宿主机端口映射，适配为容器网络配置静态路由的部署方式。
- 移除 VEX 文件和镜像 VEX 发布流程，安全扫描结果保持原样展示。

## v1.0.3 - 2026-08-15

- 运行镜像升级到 Alpine 3.24.1，移除 Docker Scout 报告的 BusyBox CVE-2025-60876。
- `golang.org/x/crypto` 升级到 v0.54.0；项目持续使用 `govulncheck` 检查实际可达漏洞。
- 保留 v1.0.2 的 NAS 数据目录权限自动修复。

## v1.0.2 - 2026-08-15

- 修复群晖等 NAS 使用宿主机目录挂载 `/data` 时，因目录所有者不匹配而无限重启的问题。
- 容器启动时仅校正专用数据目录权限，随后立即降权为非 root 用户运行平台。
- 同步正式镜像版本与部署文档。

## v1.0.1 - 2026-08-15

- Docker 首次启动自动生成并持久化内部 API 令牌，无需部署者填写。
- 移除初始化令牌及其前端表单、后端校验、Compose Secret 和旧文档。
- 首次打开网站可直接创建系统管理员；域名改为部署后在系统设置中配置。
- 同步精简 Docker 配置、备份说明和公开部署资料。

## v1.0.0 - 2026-08-15

- 发布 设备管理平台 首个正式容器版本。
- 完成重构版 NPS 节点、Client 和 SOCKS 通道接入。
- 完成客户项目、设备发现、多 Web 服务、WebSSH、权限、策略、监控和审计。
- 完成节点与 SSH 密码加密持久化、MFA、SQLite 一致性备份和离线恢复。
- 完成独立泛域名 Web 网关、HTTP/HTTPS/WebSocket/重定向兼容和实际设备验收。
- 统一正式版界面、列表分页、固定应用框架和响应式布局。
- 清理旧兼容字段、重复接口、未生效配置和测试预览内容。
